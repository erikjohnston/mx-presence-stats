# mx-presence-stats

```
$ ./presence_stats 
matrix-bot 0.1.0
Erik Johnston <mxpresence@jki.re>

USAGE:
    presence_stats [FLAGS] <CONFIG> [ARGS]

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

ARGS:
    <CONFIG>    
    [output]    
```
